steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/orange.com:orange-hubdata-cbs-dev/IMAGE', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/orange.com:orange-hubdata-cbs-dev/IMAGE']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['run', 'deploy', 'SERVICE-NAME', '--image', 'gcr.io/orange.com:orange-hubdata-cbs-dev/IMAGE', '--region', 'US', '--platform', 'managed']
images:
- gcr.io/orange.com:orange-hubdata-cbs-dev/IMAGE
